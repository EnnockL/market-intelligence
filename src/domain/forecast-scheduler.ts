export const FORECAST_SCHEDULER_VERSION="forecast-scheduler-v1";export type ScheduledJobStatus="HEALTHY"|"DEGRADED"|"FAILED"|"PAUSED";export function retryDelaySeconds(attempt:number,base=30,max=3600){return Math.min(max,base*2**Math.max(0,attempt-1))}export function healthStatus(input:{paused:boolean;consecutiveFailures:number;heartbeatAt:string|null;now:string;leaseSeconds:number}):ScheduledJobStatus{if(input.paused)return"PAUSED";if(input.consecutiveFailures>=3)return"FAILED";if(input.consecutiveFailures>0)return"DEGRADED";if(input.heartbeatAt&&Date.parse(input.now)-Date.parse(input.heartbeatAt)>input.leaseSeconds*2000)return"DEGRADED";return"HEALTHY"}
