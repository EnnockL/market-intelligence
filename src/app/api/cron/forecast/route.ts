import{NextRequest,NextResponse}from"next/server";import{createServiceClient}from"@/lib/supabase/server";import{ForecastSchedulerService}from"@/services/forecast-scheduler/service";export const runtime="nodejs";export async function GET(request:NextRequest){const secret=process.env.CRON_SECRET,authorization=request.headers.get("authorization");if(!secret||authorization!==`Bearer ${secret}`)return NextResponse.json({error:"Unauthorized"},{status:401});const service=new ForecastSchedulerService(createServiceClient(),`cron-${crypto.randomUUID()}`),jobs=[];for(let i=0;i<3;i++){const result=await service.runOnce();if(!result.claimed)break;jobs.push(result)}return NextResponse.json({scheduler:"forecast-scheduler-v1",jobs,ranAt:new Date().toISOString()})}
