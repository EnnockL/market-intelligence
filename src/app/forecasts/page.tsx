import{createServiceClient}from"@/lib/supabase/server";import styles from"./forecasts.module.css";export const dynamic="force-dynamic";
export default async function ForecastsPage(){const db=createServiceClient(),[{data:forecasts},{data:catalysts}]=await Promise.all([db.from("forecasts").select("*,assets(symbol),forecast_outcomes(*)").order("created_at",{ascending:false}).limit(100),db.from("catalysts").select("*,assets(symbol),catalyst_revisions(*)").order("created_at",{ascending:false}).limit(50)]);return <main className={styles.page}><section className={styles.hero}><p>FORWARD RESEARCH</p><h1>Forecasts & Catalysts</h1><span>Infrastructure mode. No generated probabilities and no BUY/SELL decisions.</span></section><section className={styles.panel}><header><div><p>FORECAST RECORDS</p><h2>Forecasts</h2></div><span>{forecasts?.length??0} records</span></header><div className={styles.table}><b>Asset</b><b>Horizon</b><b>Method</b><b>Status</b><b>Outcome</b><b>Quality</b>{(forecasts??[]).map((f:any)=><div className={styles.row} key={f.id}><strong>{asset(f.assets)}</strong><span>{f.horizon}</span><span>{f.forecast_method}</span><em data-status={f.status}>{f.status}</em><span>{f.forecast_outcomes?.[0]?.status??"PENDING"}</span><span>{f.data_quality??"UNKNOWN"}</span></div>)}</div></section><section className={styles.panel}><header><div><p>ACTIVE INFORMATION</p><h2>Catalyst Watch</h2></div><span>{catalysts?.length??0} catalysts</span></header>{(catalysts??[]).length?<div className={styles.cards}>{catalysts!.map((c:any)=><article key={c.id}><small>{asset(c.assets)} · {c.catalyst_type}</small><h3>{c.current_state}</h3><p>{c.materiality} materiality · {c.source_classification}</p><footer>Watch until {new Date(c.watch_until).toLocaleString("sv-SE")} · revision {c.current_revision}</footer></article>)}</div>:<div className={styles.empty}>Inga verifierbara catalysts har ingestats ännu. Systemet hittar inte på exempeldata.</div>}</section></main>}
function asset(value:any){const row=Array.isArray(value)?value[0]:value;return row?.symbol??"UNKNOWN"}
