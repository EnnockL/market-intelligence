import type { SupabaseClient } from "@supabase/supabase-js";import type { WorkerEnv } from "@/lib/env";import { ExecutionBridgeService } from "@/services/execution/bridge-service";import { createExecutionProvider } from "./execution";
export async function runExecutionBridge(db:SupabaseClient,env:WorkerEnv){const control=await db.from("execution_controls").select("mode").eq("control_key","global").single();if(control.error)throw control.error;if(control.data.mode!==env.EXECUTION_MODE)throw new Error("Execution mode mismatch");return new ExecutionBridgeService(db,createExecutionProvider(control.data.mode,env)).run()}
