import type{SupabaseClient}from"@supabase/supabase-js";import type{IngestionRepository}from"@/repositories/ingestion-repository";import{MarketEventProducerService}from"@/services/market-events/service";export async function runMarketEvents(db:SupabaseClient,repo:IngestionRepository){const runId=await repo.startRun("market_events","market-event-producers-v1");try{const result=await new MarketEventProducerService(db).run();await repo.finishRun(runId,result.evaluated);return{runId,...result};}catch(error){await repo.failRun(runId,error);throw error;}}
