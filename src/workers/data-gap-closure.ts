import type{SupabaseClient}from"@supabase/supabase-js";import type{IngestionRepository}from"@/repositories/ingestion-repository";import type{HistoricalLiquidityProvider}from"@/services/liquidity/provider";import type{TokenRiskProvider}from"@/services/token-risk/provider";import{DataGapClosureService}from"@/services/data-gap-closure/service";
export async function runDataGapClosure(db:SupabaseClient,repo:IngestionRepository,liquidity:HistoricalLiquidityProvider,risk:TokenRiskProvider,limit=4){const id=await repo.startRun("data_gap_closure",`${liquidity.name}+${risk.name}`);try{const result=await new DataGapClosureService(db,liquidity,risk,repo).run(limit);await repo.finishRun(id,result.created);return{runId:id,...result}}catch(error){await repo.recordProviderError(id,"data-gap-closure",error);await repo.failRun(id,error);throw error}}
