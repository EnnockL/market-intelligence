import type{SupabaseClient}from"@supabase/supabase-js";import type{IngestionRepository}from"@/repositories/ingestion-repository";import{HistoricalReplayService}from"@/services/historical-replay/service";
export async function runHistoricalReplay(db:SupabaseClient,repository:IngestionRepository){const ingestionRunId=await repository.startRun("historical_replay","historical-replay-v1");try{const end=new Date(),start=new Date(end.getTime()-90*86400000),result=await new HistoricalReplayService(db).run({startsAt:start.toISOString(),endsAt:end.toISOString()});await repository.finishRun(ingestionRunId,result.processed);return{ingestionRunId,...result};}catch(error){await repository.failRun(ingestionRunId,error);throw error;}}
