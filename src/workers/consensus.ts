import type{SupabaseClient}from"@supabase/supabase-js";import type{IngestionRepository}from"@/repositories/ingestion-repository";import{ConsensusService}from"@/services/consensus/service";export async function runConsensus(db:SupabaseClient,repo:IngestionRepository){const runId=await repo.startRun("consensus","consensus-conflict-policy-v1");try{const result=await new ConsensusService(db).run();await repo.finishRun(runId,result.created);return{runId,...result}}catch(error){await repo.failRun(runId,error);throw error}}
