export function JackpotRadar({data}:{data:Awaited<ReturnType<typeof import("@/data/jackpot-data").getJackpotData>>}){return<section className="radar-panel fast-flow-panel"><div className="panel-title"><div><span className="eyebrow">RESEARCH DATASET</span><h2>Jackpot Radar 🎯</h2></div><small>{data.mode.toUpperCase()}</small></div>{data.items.length?<div className="fast-flow-list">{data.items.map(x=><div key={x.id} style={{display:"grid",gridTemplateColumns:"100px 130px 120px 100px 100px 1fr",gap:14,padding:"14px 20px",borderBottom:"1px solid var(--line)"}}><strong>{x.symbol}</strong><b>{x.state}</b><span>{x.raw??"UNKNOWN"} raw</span><span>{x.independent??"UNKNOWN"} independent</span><span>{x.coverage===null?"UNKNOWN":`${x.coverage}%`} coverage</span><small>Safety {x.safety} · no execution</small></div>)}</div>:<div className="discovery-empty"><strong>No jackpot candidates yet</strong><span>Collector stores evidence only; it never creates a BUY decision.</span></div>}</section>}
