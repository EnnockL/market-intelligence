import Link from "next/link";import {DataStatus} from "./data-status";import type{FastFlowData}from "@/data/fast-flow-data";
export function FastFlowPanel({data}:{data:FastFlowData}){return <section className="radar-panel fast-flow-panel"><div className="panel-title fast-flow-title"><div><span className="eyebrow">EVIDENCE-BACKED</span><h2>Fast Flow ⚡</h2></div><DataStatus mode={data.mode} updatedAt={data.updatedAt} message={data.message}/></div>{data.items.length?<div className="fast-flow-list">{data.items.map(item=><Link href={`/opportunities/${item.id}`} key={item.id}><strong>{item.symbol}</strong><span>{item.walletCount} verified wallets</span><b>{item.state.replaceAll("_"," ")}</b><span>score {item.score||"—"}</span><span>risk {item.riskScore??"UNKNOWN"}</span><span>{item.evidenceCount} evidence</span><small>{item.blockers.length?item.blockers.slice(0,2).join(" · "):"SAFETY PASS"}</small></Link>)}</div>:<div className="discovery-empty"><strong>No verified convergence yet</strong><span>{data.message}</span></div>}</section>}
