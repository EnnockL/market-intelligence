import type{SupabaseClient}from"@supabase/supabase-js";import{createEventEnvelope}from"@/domain/events";import{canonicalizeNewsUrl,newsContentHash,newsRevisionKey,NEWS_TRUST_VERSION}from"@/domain/news";import type{NewsProvider}from"./provider";import{ForecastCatalystService}from"@/services/forecast-catalyst/service";import{PostgresOutboxTransport}from"@/services/events/postgres-outbox-transport";
export class NewsIngestionService{constructor(private db:SupabaseClient,private provider:NewsProvider){}
async run(symbols:string[],now=new Date().toISOString(),limit=50){const from=new Date(Date.parse(now)-86400000).toISOString(),result=await this.provider.fetchNews({symbols,from,to:now,limit});let inserted=0,revisions=0,catalysts=0,unchanged=0;const transport=new PostgresOutboxTransport(this.db),watch=new ForecastCatalystService(this.db);
for(const item of result.observations){const{data:asset,error:assetError}=await this.db.from("assets").select("id").eq("kind","stock").eq("symbol",item.symbol).maybeSingle();if(assetError)throw assetError;if(!asset)continue;const canonicalUrl=canonicalizeNewsUrl(item.url),source=await this.resolveSource(canonicalUrl),contentHash=newsContentHash({headline:item.headline,summary:item.summary,canonicalUrl});let{data:observation,error:lookupError}=await this.db.from("news_observations").select("id").eq("asset_id",asset.id).eq("observation_key",item.observationKey).maybeSingle();if(lookupError)throw lookupError;if(!observation){const canonical=await this.db.from("news_observations").select("id").eq("asset_id",asset.id).eq("canonical_url",canonicalUrl).limit(1).maybeSingle();if(canonical.error)throw canonical.error;observation=canonical.data}
if(!observation){const saved=await this.db.from("news_observations").insert({observation_key:item.observationKey,observation_version:"news-observation-v1",asset_id:asset.id,provider:item.provider,provider_article_id:item.providerArticleId,headline:item.headline,summary:item.summary,url:item.url,canonical_url:canonicalUrl,source_name:item.sourceName,source_id:source?.id??null,source_classification:source?.source_classification??item.sourceClassification,trust_policy_version:"news-source-trust-v1",occurred_at:item.occurredAt,observed_at:item.observedAt,available_at:item.availableAt,data_quality:item.dataQuality,raw_payload:item.rawPayload}).select("id").single();if(saved.error?.code==="23505"){const retry=await this.db.from("news_observations").select("id").eq("provider",item.provider).eq("provider_article_id",item.providerArticleId).eq("asset_id",asset.id).single();if(retry.error)throw retry.error;observation=retry.data}else{if(saved.error)throw saved.error;observation=saved.data;inserted++}}
const observationId=observation!.id as string,{data:latest,error:revisionError}=await this.db.from("news_article_revisions").select("revision_number,content_hash").eq("news_observation_id",observationId).order("revision_number",{ascending:false}).limit(1).maybeSingle();if(revisionError)throw revisionError;if(latest?.content_hash===contentHash){unchanged++;continue}const revisionNumber=(latest?.revision_number??0)+1,revisionKey=newsRevisionKey({observationId,contentHash}),revisionSaved=await this.db.from("news_article_revisions").insert({revision_key:revisionKey,news_observation_id:observationId,revision_number:revisionNumber,headline:item.headline,summary:item.summary,canonical_url:canonicalUrl,content_hash:contentHash,occurred_at:item.occurredAt,observed_at:item.observedAt,available_at:item.availableAt,provider:item.provider,provider_article_id:item.providerArticleId,source_id:source?.id??null,source_classification:source?.source_classification??item.sourceClassification,evidence_refs:[],raw_payload:item.rawPayload}).select("id").single();if(revisionSaved.error?.code==="23505"){unchanged++;continue}if(revisionSaved.error)throw revisionSaved.error;revisions++;const revisionId=revisionSaved.data.id as string;const entity=await this.db.from("news_entity_matches").insert({news_revision_id:revisionId,entity_type:"asset",entity_id:asset.id,match_method:"PROVIDER_SYMBOL",confidence:100,available_at:item.availableAt,match_version:"news-entity-match-v1"});if(entity.error)throw entity.error;
const event=createEventEnvelope({eventType:revisionNumber===1?"news.observed":"news.revised",entityType:"news_revision",entityId:revisionId,assetId:asset.id,occurredAt:item.occurredAt,observedAt:item.observedAt,availableAt:item.availableAt,provider:item.provider,sourceReference:canonicalUrl,dataQuality:item.dataQuality,confidence:null,payload:{headline:item.headline,sourceName:item.sourceName,sourceClassification:source?.source_classification??item.sourceClassification,observationId,revisionNumber,contentHash,trustVersion:NEWS_TRUST_VERSION},correlationId:observationId,causationId:null});await transport.publish(event);
if(revisionNumber===1){const catalyst=await watch.ingest({assetId:asset.id,catalystType:"NEWS_OBSERVATION",materiality:"UNKNOWN",novelty:"UNKNOWN",sourceClassification:source?.source_classification??item.sourceClassification,sourceConfidence:null,watchWindow:"SHORT",occurredAt:item.occurredAt,observedAt:item.observedAt,availableAt:item.availableAt,sourceReference:canonicalUrl,evidenceRefs:[event.eventId],details:{newsObservationId:observationId,newsRevisionId:revisionId,headline:item.headline,producerAssessment:"NONE"}});if(!catalyst.reused)catalysts++}}
return{fetched:result.observations.length,inserted,revisions,unchanged,catalysts,remaining:result.remaining,resetAt:result.resetAt}}
private async resolveSource(canonicalUrl:string){let domain:string;try{domain=new URL(canonicalUrl).hostname.replace(/^www\./,"")}catch{return null}const{data,error}=await this.db.from("news_sources").select("id,source_classification,domain").eq("active",true);if(error)throw error;return(data??[]).filter((row:any)=>domain===row.domain||domain.endsWith(`.${row.domain}`)).sort((a:any,b:any)=>b.domain.length-a.domain.length)[0]??null}}
