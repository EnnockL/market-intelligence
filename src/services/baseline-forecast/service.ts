import type{SupabaseClient}from"@supabase/supabase-js";import{BASELINE_COHORT_POLICY,BASELINE_FEATURE_VERSION,BASELINE_FORECAST_VERSION,buildHistoricalBaseline,calculateForwardReturn,targetTime,type BaselineFeature,type HistoricalExample}from"@/domain/baseline-forecast";import{createEventEnvelope,deterministicDigest}from"@/domain/events";import{forecastKey,horizonMs,type ForecastHorizon}from"@/domain/forecast";import{PostgresOutboxTransport}from"@/services/events/postgres-outbox-transport";
type Observation={id:string;asset_id:string;observed_at:string;ingested_at:string;price_usd:number|null;volume_24h_usd:number|null;liquidity_usd:number|null;market_cap_usd:number|null;confidence:number};
export class BaselineForecastService{private transport;constructor(private db:SupabaseClient){this.transport=new PostgresOutboxTransport(db)}
 async run(now=new Date().toISOString()){const runKey=deterministicDigest({version:BASELINE_FORECAST_VERSION,now:now.slice(0,13)});let{data:run,error}=await this.db.from("baseline_forecast_runs").select("*").eq("run_key",runKey).maybeSingle();if(error)throw error;if(run?.status==="COMPLETED")return{runId:run.id,reused:true,evaluated:run.forecasts_evaluated,available:run.forecasts_available};if(!run){const created=await this.db.from("baseline_forecast_runs").insert({run_key:runKey,model_version:BASELINE_FORECAST_VERSION,feature_version:BASELINE_FEATURE_VERSION,cohort_policy_version:BASELINE_COHORT_POLICY.version,information_cutoff_at:now,status:"RUNNING"}).select("*").single();if(created.error)throw created.error;run=created.data}try{const{data:targets,error:tError}=await this.db.from("baseline_forecast_pending_targets").select("*").lte("information_cutoff_at",now).order("information_cutoff_at").limit(25);if(tError)throw tError;let evaluated=0,available=0;for(const target of targets??[]){const result=await this.evaluate(target);evaluated++;if(result.status==="AVAILABLE")available++}const saved=await this.db.from("baseline_forecast_runs").update({status:"COMPLETED",completed_at:new Date().toISOString(),forecasts_evaluated:evaluated,forecasts_available:available}).eq("id",run.id);if(saved.error)throw saved.error;return{runId:run.id,reused:false,evaluated,available}}catch(cause){await this.db.from("baseline_forecast_runs").update({status:"FAILED",last_error:cause instanceof Error?cause.message:String(cause)}).eq("id",run.id);throw cause}}
 private async evaluate(target:any){const horizon=target.horizon as ForecastHorizon,cutoff=target.information_cutoff_at;const{data:rows,error}=await this.db.from("crypto_market_observations").select("id,asset_id,observed_at,ingested_at,price_usd,volume_24h_usd,liquidity_usd,market_cap_usd,confidence").lte("observed_at",cutoff).lte("ingested_at",cutoff).order("observed_at").limit(10000);if(error)throw error;const observations=(rows??[]).map(mapObservation),byAsset=group(observations),targetRows=byAsset.get(target.asset_id)??[],current=lastAt(targetRows,cutoff),targetFeature=featureAt(targetRows,current);const examples=historicalExamples(byAsset,horizon,cutoff);const result=buildHistoricalBaseline(targetFeature,examples,horizon,cutoff),key=forecastKey({assetId:target.asset_id,catalystId:target.catalyst_id,horizon,cutoff,version:BASELINE_FORECAST_VERSION});const existing=await this.db.from("forecasts").select("id,status").eq("forecast_key",key).maybeSingle();if(existing.error)throw existing.error;if(existing.data)return{status:existing.data.status,reused:true};const evidence=current?[current.id]:[];const inserted=await this.db.from("forecasts").insert({forecast_key:key,asset_id:target.asset_id,opportunity_id:target.opportunity_id,candidate_id:target.candidate_id,catalyst_id:target.catalyst_id,forecast_version:BASELINE_FORECAST_VERSION,forecast_method:result.status==="AVAILABLE"?"HISTORICAL_BASELINE":"INSUFFICIENT_DATA",model_version:BASELINE_FORECAST_VERSION,feature_set_version:BASELINE_FEATURE_VERSION,training_cutoff_at:cutoff,information_cutoff_at:cutoff,available_at:new Date().toISOString(),horizon,status:result.status,reason:result.reason,expected_return:result.expectedReturn,lower_bound:result.lowerBound,upper_bound:result.upperBound,probability_positive:result.probabilityPositive,probability_2x:result.probability2x,probability_5x:result.probability5x,probability_10x:result.probability10x,confidence:result.status==="AVAILABLE"?Math.min(100,Math.round(result.sampleSize/BASELINE_COHORT_POLICY.minimumSampleSize*70)):null,data_quality:result.dataQuality,evidence_refs:evidence,agent_inputs:{cohortPolicy:BASELINE_COHORT_POLICY.version,sampleSize:result.sampleSize,modelHash:result.modelHash,sourceForecastId:target.id},market_regime:null}).select("id").single();if(inserted.error)throw inserted.error;const forecastId=inserted.data.id,snapshotKey=deterministicDigest({forecastId,feature:targetFeature,version:BASELINE_FEATURE_VERSION});const snapshot=await this.db.from("baseline_feature_snapshots").insert({snapshot_key:snapshotKey,forecast_id:forecastId,asset_id:target.asset_id,feature_version:BASELINE_FEATURE_VERSION,information_cutoff_at:cutoff,available_at:new Date().toISOString(),price_momentum_5m:targetFeature.priceMomentum5m,volume_multiple_5m:targetFeature.volumeMultiple5m,liquidity_usd:targetFeature.liquidityUsd,market_cap_usd:targetFeature.marketCapUsd,data_quality:targetFeature.dataQuality,feature_payload:targetFeature,feature_hash:deterministicDigest(targetFeature)});if(snapshot.error)throw snapshot.error;if(result.members.length){const exampleMap=new Map(examples.map(x=>[x.sourceId,x]));const cohort=await this.db.from("baseline_cohort_members").insert(result.members.map(member=>{const x=exampleMap.get(member.sourceId)!;return{forecast_id:forecastId,source_observation_id:x.sourceId,anchor_at:x.anchorAt,outcome_at:x.outcomeAt,return_pct:x.returnPct,feature_distance:0,available_at:x.availableAt,member_hash:deterministicDigest({forecastId,sourceId:x.sourceId})}}));if(cohort.error)throw cohort.error}const emittedAt=new Date().toISOString();await this.transport.publish(createEventEnvelope({eventType:"forecast.baseline_created",entityType:"forecast",entityId:forecastId,assetId:target.asset_id,occurredAt:emittedAt,observedAt:emittedAt,availableAt:emittedAt,provider:"baseline-forecast",sourceReference:`baseline:${key}`,dataQuality:result.dataQuality??0,confidence:null,payload:{status:result.status,reason:result.reason,horizon,sampleSize:result.sampleSize,modelVersion:BASELINE_FORECAST_VERSION},correlationId:target.catalyst_id,causationId:null}));return result}}
function mapObservation(x:any):Observation{return{...x,price_usd:num(x.price_usd),volume_24h_usd:num(x.volume_24h_usd),liquidity_usd:num(x.liquidity_usd),market_cap_usd:num(x.market_cap_usd),confidence:Number(x.confidence)}}function num(x:unknown){const n=Number(x);return x===null||x===undefined||!Number.isFinite(n)?null:n}function group(rows:Observation[]){const out=new Map<string,Observation[]>;for(const row of rows){const list=out.get(row.asset_id)??[];list.push(row);out.set(row.asset_id,list)}return out}function lastAt(rows:Observation[],at:string){return[...rows].reverse().find(x=>x.observed_at<=at)??null}function featureAt(rows:Observation[],current:Observation|null):BaselineFeature{if(!current)return{priceMomentum5m:null,volumeMultiple5m:null,liquidityUsd:null,marketCapUsd:null,dataQuality:0};const prior=lastAt(rows,new Date(Date.parse(current.observed_at)-300000).toISOString()),price=prior?.price_usd&&current.price_usd?(current.price_usd/prior.price_usd-1)*100:null,volume=prior?.volume_24h_usd&&current.volume_24h_usd?current.volume_24h_usd/prior.volume_24h_usd:null;return{priceMomentum5m:price,volumeMultiple5m:volume,liquidityUsd:current.liquidity_usd,marketCapUsd:current.market_cap_usd,dataQuality:Math.min(current.confidence,prior?.confidence??current.confidence)}}
function historicalExamples(byAsset:Map<string,Observation[]>,horizon:ForecastHorizon,cutoff:string){const out:HistoricalExample[]=[];for(const[assetId,rows]of byAsset)for(const anchor of rows){if(!anchor.price_usd)continue;const desired=targetTime(anchor.observed_at,horizon),max=new Date(Date.parse(desired)+Math.max(60000,horizonMs(horizon)*.1)).toISOString(),exit=rows.find(x=>x.observed_at>=desired&&x.observed_at<=max&&x.price_usd&&x.ingested_at<=cutoff);if(!exit?.price_usd)continue;const features=featureAt(rows,anchor),availableAt=[anchor.ingested_at,exit.ingested_at].sort().at(-1)!;out.push({sourceId:anchor.id,assetId,anchorAt:anchor.observed_at,outcomeAt:exit.observed_at,availableAt,features,returnPct:calculateForwardReturn(anchor.price_usd,exit.price_usd)})}return out}
