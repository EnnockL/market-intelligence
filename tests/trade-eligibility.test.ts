import{describe,expect,it}from"vitest";import{evaluateTradeEligibility,type TradeProposal}from"@/domain/trade-eligibility";
const proposal:TradeProposal={proposalId:"p1",assetId:"a1",sourceType:"policy",sourceId:"s1",instrumentId:"BTC-USDT",side:"BUY",orderType:"LIMIT",quoteAmountSek:100,quantity:.001,referencePrice:100000,limitPrice:100000,stopPrice:95000,targetPrice:110000,maxSlippageBps:25,informationCutoffAt:"2026-08-24T10:00:00.000Z",availableAt:"2026-08-24T09:59:59.000Z",expiresAt:"2026-08-24T10:01:00.000Z",metaReady:true,directionalSupport:"BULLISH",criticalSafety:"PASS",liquidityStatus:"PASS",riskStatus:"PASS",dataAgeMs:1000,evidenceRefs:["meta:1"]};
describe("trade eligibility",()=>{it("accepts a complete proposal",()=>expect(evaluateTradeEligibility(proposal).decision).toBe("ELIGIBLE"));it("separates missing data from rejection",()=>{const result=evaluateTradeEligibility({...proposal,quantity:null});expect(result.decision).toBe("DATA_BLOCKED");expect(result.unknown).toContain("BASE_QUANTITY")});it("rejects directional conflict",()=>{const result=evaluateTradeEligibility({...proposal,directionalSupport:"BEARISH"});expect(result.decision).toBe("REJECTED");expect(result.failed).toContain("DIRECTIONAL_SUPPORT")});it("blocks unknown critical risk",()=>expect(evaluateTradeEligibility({...proposal,riskStatus:"UNKNOWN"}).decision).toBe("DATA_BLOCKED"));it("rejects future proposal data",()=>expect(()=>evaluateTradeEligibility({...proposal,availableAt:"2026-08-24T10:00:01.000Z"})).toThrow("Future trade proposal"));it("is deterministic",()=>expect(evaluateTradeEligibility(proposal).resultHash).toBe(evaluateTradeEligibility(proposal).resultHash))});
