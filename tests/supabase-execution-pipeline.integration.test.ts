import{beforeAll,describe,expect,it}from"vitest";import{createClient,type SupabaseClient}from"@supabase/supabase-js";const suite=process.env.RUN_SUPABASE_INTEGRATION==="1"?describe:describe.skip;let db:SupabaseClient;suite("Supabase execution pipeline scheduler",()=>{beforeAll(()=>{db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false,autoRefreshToken:false}})});it("registers one enabled leased pipeline job",async()=>{const{data,error}=await db.from("scheduled_jobs").select("job_type,interval_seconds,lease_seconds,enabled").eq("job_key","execution-pipeline-1m").single();expect(error).toBeNull();expect(data).toMatchObject({job_type:"EXECUTION_PIPELINE",interval_seconds:60,lease_seconds:55,enabled:true})})});
