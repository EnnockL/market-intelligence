import{describe,expect,it}from"vitest";import{evaluateSpecialist,type SpecialistInput}from"../src/domain/specialist-agents";const input=(specialist:SpecialistInput["specialist"],context:Record<string,unknown>):SpecialistInput=>({specialist,assetId:"a",informationCutoffAt:"2026-01-01T00:00:00Z",context,evidenceRefs:["e"],knowledgeRuleRefs:["k@1"],knowledgeRequestHash:"r"});describe("specialist agents",()=>{it("evaluates momentum components deterministically",()=>expect(evaluateSpecialist(input("MOMENTUM",{priceChangePct:10,volumeMultiple:2,liquidityUsd:50000,dataQuality:90})).conclusion).toBe("SUPPORTED"));it("keeps missing momentum evidence unknown",()=>expect(evaluateSpecialist(input("MOMENTUM",{})).conclusion).toBe("INSUFFICIENT_DATA"));it("rejects deterministic high risk",()=>expect(evaluateSpecialist(input("TOKEN_RISK",{riskClassification:"HIGH_RISK",riskCoverage:90})).conclusion).toBe("NOT_SUPPORTED"));it("does not invent fundamentals",()=>{const r=evaluateSpecialist(input("FUNDAMENTAL",{}));expect(r.conclusion).toBe("INSUFFICIENT_DATA");expect(r.components.every(x=>x.status==="UNKNOWN")).toBe(true)});it("is reproducible",()=>expect(evaluateSpecialist(input("MOMENTUM",{})).analysisHash).toBe(evaluateSpecialist(input("MOMENTUM",{})).analysisHash))});
