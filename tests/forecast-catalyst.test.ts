import{describe,expect,it}from"vitest";import{assertCatalystTransition,canTransitionCatalyst,forecastKey,outcomeMetrics,watchUntil}from"../src/domain/forecast";
describe("forecast and catalyst infrastructure",()=>{it("enforces catalyst lifecycle",()=>{expect(canTransitionCatalyst("WATCHING","ACTIVATING")).toBe(true);expect(()=>assertCatalystTransition("EXPIRED","ACTIVATING")).toThrow("Invalid catalyst transition")});it("keeps source confidence separate from materiality through deterministic identity",()=>{const a=forecastKey({assetId:"a",catalystId:"c",horizon:"30m",cutoff:"2026-01-01T00:00:00Z",version:"v1"});expect(a).toBe(forecastKey({assetId:"a",catalystId:"c",horizon:"30m",cutoff:"2026-01-01T00:00:00Z",version:"v1"}))});it("uses bounded watch windows",()=>expect(watchUntil("2026-01-01T00:00:00Z","SHORT")).toBe("2026-01-04T00:00:00.000Z"));it("calculates measurable outcomes without inventing a forecast",()=>{const result=outcomeMetrics({expected:null,lower:null,upper:null,entry:100,exit:110,path:[100,120,90,110]});expect(result.actualReturn).toBeCloseTo(10);expect(result.absoluteError).toBeNull();expect(result.maxFavorableExcursion).toBeCloseTo(20);expect(result.maxAdverseExcursion).toBeCloseTo(-10)})});
