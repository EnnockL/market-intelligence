import{describe,expect,it}from"vitest";import{evaluateBridgeReadiness}from"@/domain/execution-bridge";
const input={eligibilityDecision:"ELIGIBLE"as const,proposalExpiresAt:"2026-08-24T10:01:00.000Z",riskStatus:"KNOWN"as const,riskAvailableAt:"2026-08-24T09:59:59.000Z",accountObservedAt:"2026-08-24T09:59:55.000Z",cutoffAt:"2026-08-24T10:00:00.000Z",maxAccountAgeMs:15000};
describe("execution bridge",()=>{it("is ready only with current known evidence",()=>expect(evaluateBridgeReadiness(input).decision).toBe("READY"));it("blocks expired proposals",()=>expect(evaluateBridgeReadiness({...input,proposalExpiresAt:input.cutoffAt}).decision).toBe("BLOCKED"));it("data-blocks unknown risk",()=>expect(evaluateBridgeReadiness({...input,riskStatus:"UNKNOWN",riskAvailableAt:null}).decision).toBe("DATA_BLOCKED"));it("blocks stale account observations",()=>expect(evaluateBridgeReadiness({...input,accountObservedAt:"2026-08-24T09:59:40.000Z"}).decision).toBe("BLOCKED"));it("rejects future account observations",()=>expect(evaluateBridgeReadiness({...input,accountObservedAt:"2026-08-24T10:00:01.000Z"}).decision).toBe("BLOCKED"));it("is deterministic",()=>expect(evaluateBridgeReadiness(input).resultHash).toBe(evaluateBridgeReadiness(input).resultHash))});
