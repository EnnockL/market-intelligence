import{beforeAll,describe,expect,it}from"vitest";import{createClient,type SupabaseClient}from"@supabase/supabase-js";const suite=process.env.RUN_SUPABASE_INTEGRATION==="1"?describe:describe.skip;let db:SupabaseClient;
suite("Supabase execution bridge",()=>{beforeAll(()=>{db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false,autoRefreshToken:false}})});it("exposes the bridge store",async()=>{const result=await db.from("execution_bridge_attempts").select("id,outcome,bridge_policy_version").limit(1);expect(result.error).toBeNull();expect(Array.isArray(result.data)).toBe(true)});it("keeps live execution forbidden",async()=>{const result=await db.from("execution_controls").update({live_execution_enabled:true}).eq("control_key","global");expect(result.error).not.toBeNull()})});
